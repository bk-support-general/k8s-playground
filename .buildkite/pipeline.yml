steps:
  - label: ":pipeline:" 
    command: whoami 
    plugins:
      - kubernetes: 
          checkout:
            name: "skippy"
            skip: true
          podSpecPatch:  
              containers:
              - name: "container-0"
                image: "alpine:latest" 