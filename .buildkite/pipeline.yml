steps:
  - label: ":pipeline:" 
    command: whoami 
    plugins:
      - kubernetes: 
          podSpecPatch:  
              containers:
              - name: "container-0"
                image: "alpine:latest" 